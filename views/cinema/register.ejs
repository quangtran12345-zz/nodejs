<% layout('../layout') -%>
<div class="loader"></div>
<header class="navbar navbar-inverse navbar-fixed-top bs-docs-nav" role="banner" ng-controller="RegisterController">
        <div class="container-fluid">
            <div class="navbar-header">
                <button class="navbar-toggle" type="button" data-toggle="collapse" data-target=".bs-navbar-collapse">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a href="/" class="navbar-brand">
                    <img id="brand-img" width="66px;" src="/images/movie-icon.png" />
                    <span class="brand">My<strong>Cinema</strong>
                    </span>
            </div>
            <nav class="collapse navbar-collapse bs-navbar-collapse" role="navigation">            
                <ul class="nav navbar-nav navbar-right">
                        <li ng-show="user._id" ng-cloak>
                                <a href="/user/{{user._id}}">
                                    <i class="fa fa-user" aria-hidden="true"></i> {{user.username}}</a>
                            </li>
                    <li ng-show="user._id" ng-cloak>
                        <a href="/create">
                            <i class="fa fa-user-plus"></i> Tạo phim</a>
                    </li>
                    <li ng-show="!user._id" ng-cloak>
                        <a href="/register">
                            <i class="fa fa-user-plus"></i> Đăng ký</a>
                    </li>                   
                    <li ng-show="user._id" ng-cloak>
                            <a href="" ng-click="signout()">
                                <i class="fa fa-sign-out"></i> Đăng xuất</a>
                        </li>
                    <li ng-show="!user._id" ng-cloak>
                        <a href="/login">
                            <i class="fa fa-sign-in"></i> Đăng nhập</a>
                    </li>

                </ul>
            </nav>
        </div>
    </header>
    <div class="parent" ng-controller="RegisterController">
        <div class="container">
            <div class="col-md-6 col-md-offset-3">
                <form name="createForm" ng-validate="validationOptions" class="sign-up-form">
                    <div class="col-md-12">
                        <h1 class="text-center"><strong>Đăng Ký</strong></h1>
                    </div>
                    <div class="form-group">
                        <label for="name">Tên người dùng:</label>
                        <input name="name" ng-model="username" type="text" class="form-control" placeholder="Nhập tên người dùng">
                    </div>
                    <div class="form-group">
                        <label for="email">Email:</label>
                        <input name="email" ng-model="email" type="text" class="form-control" id="email" placeholder="Email">
                    </div>
                    <div class="form-group">
                        <label for="pwd">Mật khẩu:</label>
                        <input name="password" ng-model="password" type="password" class="form-control" id="pwd" placeholder="Nhập mật khẩu">
                    </div>
                    <div class="form-group">
                        <label for="pwd">Nhập lại mật khẩu:</label>
                        <input name="confirmPassword" type="password" class="form-control" placeholder="Xác nhận mật khẩu">
                    </div>
                    <button type="submit" ng-click="createUser()" id="register-button" class="create-form-button btn btn-primary">Đăng ký</button>
                </form>
            </div>
        </div>
    </div>
    </div>
    <script src="/javascripts/auth/register.js"></script>